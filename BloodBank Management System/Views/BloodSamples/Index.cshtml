@model IEnumerable<BloodBank_Management_System.Models.BloodSample>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Donor.DonorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BloodGroups)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BloodQuentity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Donor.DonorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BloodGroups)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BloodQuentity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BloodSampleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BloodSampleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BloodSampleId })
        </td>
    </tr>
}

</table>
